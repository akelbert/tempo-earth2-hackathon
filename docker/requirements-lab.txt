# JupyterHub single-user server, Glue, and the geoscience stack for the
# Earth-2 Lab attendee image.
#
# Everything is pinned. The release tag and the resolved image digest must
# describe the same software, and "it worked when I built it last week" is not
# an acceptable answer three days before the event.
#
# Not pinned here: torch, numpy and the CUDA stack. Those come from the NGC
# base image and from Earth2Studio's own resolution. If a package below forces
# a torch or numpy change, the verification step in the Dockerfile fails the
# build - that is intentional.

# --- JupyterHub / JupyterLab -------------------------------------------------
# jupyterhub must be compatible with the Hub deployed by the Zero-to-JupyterHub
# chart. Check the chart's Hub version before changing this pin.
jupyterhub==5.5.1
jupyterlab==4.6.3
notebook==7.6.2
jupyter-server-proxy==4.5.0
jupyter-resource-usage==1.3.0
jupyterlab-git==0.54.1
nbclient==0.11.0
nbconvert==7.17.1

# --- Widgets -----------------------------------------------------------------
ipywidgets==8.1.9
ipympl==0.10.0
bqplot==0.13.1
bqplot-image-gl==1.10.2
ipyleaflet==0.20.0

# --- Glue --------------------------------------------------------------------
# glue-jupyter describes itself as experimental. It is scaffolding for the
# guided path, not a load-bearing dependency: every workshop notebook must
# still produce its result with matplotlib alone if a Glue viewer fails.
glue-core==1.27.0
glue-jupyter==0.29.0

# --- Geoscience / analysis ---------------------------------------------------
matplotlib==3.11.1
cartopy==0.25.0
h5netcdf==1.8.1
# Earth2Studio constrains netCDF4 to <1.7.3 (Unidata/netcdf4-python#1438).
netCDF4==1.7.2
bottleneck==1.6.0
dask[array,diagnostics]==2026.7.1
flox==0.11.2

# --- NASA data access --------------------------------------------------------
# earthaccess is a preparation-time dependency (staging TEMPO subsets). It is
# included in the attendee image so that a curious attendee can pull their own
# granule, but the event-day path reads the staged copy from Cloud Storage.
earthaccess==0.18.0

# --- Cloud Storage -----------------------------------------------------------
gcsfs==2026.8.0
fsspec==2026.7.0
